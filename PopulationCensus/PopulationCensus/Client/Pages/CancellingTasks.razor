@page "/cancelling-tasks"


<h3>CancellingTasks</h3>

<br />

<div style="border-style: solid; padding: 50px; height: 190px;">
    
<div><span>Cancelling a simple delay task</span></div>

<br />

<button @onclick="@StartButton_SimpleDelayTask_Click" disabled="@IsStartButtonDisabled_SimpleDelayTask">Start</button>

<button @onclick="@CancelButton_SimpleDelayTask_Click" disabled="@IsCancelButtonDisabled_SimpleDelayTask">Cancel</button>

<div>@MessageSimpleDelayTask</div>

</div>

<br />
<br />
<br />

<div style="border-style: solid; padding: 50px; height: 190px;">
    
<div><span>Cancelling long Http Request</span></div>

<br />

<button @onclick="@StartButton_LongHttpRequest_Click" disabled="@IsStartButtonDisabled_LongHttpRequest">Start</button>

<button @onclick="@CancelButton_LongHttpRequest_Click" disabled="@IsCancelButtonDisabled_LongHttpRequest">Cancel</button>

<div>@MessageLongHttpRequest</div>

</div>

<br />
<br />
<br />

<div style="border-style: solid; padding: 50px; height: 190px;">

<div><span>Cancelling long DB Request</span></div>

<br />

<button @onclick="@StartButton_LongDBRequest_Click" disabled="@IsStartButtonDisabled_LongDBRequest">Start</button>

<button @onclick="@CancelButton_LongDBRequest_Click" disabled="@IsCancelButtonDisabled_LongDBRequest">Cancel</button>

<div>@MessageLongDBRequest</div>

</div> 

<br />
<br />
<br />

<div style="border-style: solid; padding: 50px; height: 190px;">
    
<div><span>Cancel a long http operation delay</span></div>

<br />

<button @onclick="@StartButton_LongHTTPRequestDelay_Click" disabled="@IsStartButtonDisabled_LongHTTPRequestDelay">Start</button>

<button @onclick="@CancelButton_LongHTTPRequestDelay_Click" disabled="@IsCancelButtonDisabled_LongHTTPRequestDelay">Cancel</button>

<div>@MessageLongHTTPRequestDelay</div>

</div>

<br />
<br />
<br />

<div style="border-style: solid; padding: 50px; height: 190px;">
    
<div><span>Cancel after a certain period of time</span></div>

<br />

<button @onclick="@StartButton_TimeOut_Click" disabled="@IsStartButtonDisabled_TimeOut">Start</button>

<button @onclick="@CancelButton_TimeOut_Click" disabled="@IsCancelButtonDisabled_TimeOut">Cancel</button>

<div>@MessageTimeOut</div>

</div>


<br />
<br />
<br />

<div style="border-style: solid; padding: 50px; height: 190px;">
    
<div><span>Get data one by one Cancel after a certain period of time</span></div>

<br />

<button @onclick="@StartButton_OneByOne_Click" disabled="@IsStartButtonDisabled_OneByOne">Start</button>

<button @onclick="@CancelButton_OneByOne_Click" disabled="@IsCancelButtonDisabled_OneByOne">Cancel</button>

<div>@MessageOneByOne</div>

</div>

@if (forecasts == null)
{
    <p><em>Loading...</em></p>
}
else
{
    <table class="table">
        <thead>
            <tr>
                <th>Date</th>
                <th>Temp. (C)</th>
                <th>Temp. (F)</th>
                <th>Summary</th>
            </tr>
        </thead>
        <tbody>
            @foreach (var forecast in forecasts)
            {
                <tr>
                    <td>@forecast.AgeId</td>
                    <td>@forecast.AreaId</td>
                    <td>@forecast.EthnicityId</td>
                </tr>
            }
        </tbody>
    </table>
}